import { describe, it } from "vitest";

// Active suite lives at ./tabbed-container/ directory.
// Vitest requires at least one test in every collected file, so this stub
// keeps the legacy filename routable while pointing readers to the new home.
describe("tabbed-container (legacy file path)", () => {
	it.skip("see tests/views/tabbed-container/* for the active suite", () => {});
});
