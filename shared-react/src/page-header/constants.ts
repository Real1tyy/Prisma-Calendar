/**
 * Color value persisted by the picker when the user has not chosen a color.
 * Treated as "no override" by both the rendered toolbar and the manager modal.
 */
export const DEFAULT_COLOR_SENTINEL = "#000000";

/** Fallback color shown in the edit form when an action has no color set. */
export const FALLBACK_EDIT_COLOR = "#ffffff";
