### Improved

- The Properties tab now explains property ordering with worked before/after examples, spells out that your own keys keep their values and their relative order, and shows how the keys above the first Prisma property decide where the block lands. Every explanation panel on the tab — property order, event types, recurring events, display properties — folds away once you've read it, and stays folded on that device.
